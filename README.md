# The structure should be like this:
-docker/
    -docker-compose.yml
    -Dockerfile
    -hibernate.cfg.xml
    -Apache/
        -Dockerfile
        -httpd.conf
    -mysql/
        -addData.sql
        -CreateDB.sql
        -Dockerfile
        -run_db.sh
